添加系统变量

    echo 'export PATH=$PATH:/usr/include' >> /etc/profile
    source /etc/profile

第一个c程序 hello.c

    #include <stdio.h>
    int main()
    {
        printf("hello world!");
    }

保存后执行：

    gcc -o hello hello.c
    ./hello

